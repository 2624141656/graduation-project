*note.001:
    Maven是基于项目对象模型(POM)，可以通过一小段描述信息来管理项目的构建，报告和文档的软件项目管理工具。
    Maven 除了以程序构建能力为特色之外，还提供高级项目管理工具。
*note.002:
    IoC(inversion of control)控制反转，也就是说，控制权由对象转移，对象自己变成了被动的接受。
    DI(dependency injection)依赖注入
    AOP(aspect oriented programming)面向切面编程
*note.003:
    Logback是由log4j创始人设计。当前分成三个模块：logback-core,logback- classic和logback-access。logback-core是其它两个模块的基础模块。
    由于Logback-classic依赖slf4j-api.jar和logback-core.jar，所以要把slf4j-api.jar、logback-core.jar、logback-classic.jar，添加到要引入Logbac日志管理的项目的class path中.
*note.004:
    curator是zookeeper中的类似于guava对于java的意义一样，提供了丰富的封装，异常处理，
    提供了fluent编程模型，提供了master选举，分布式锁，分布式基数，分布式barrier，可以很方便的为日常生产所使用。
*note.005:
    netty-all建立管理长连接、推送、通知、数据传输、自定义协议等等
    Netty是一个异步通信、事件驱动基于NIO编写的高性能高并发的java网络编程框架。
    参考文档地址：https://blog.csdn.net/luo4105/article/details/73817616?locationNum=2&fps=1
*note.006:
    数据库连接池,用以存储账号密码等安全性较高的
    参考文档地址：http://www.cnblogs.com/niejunlei/p/5977895.html
*note.007:
    1、Json已经成为当前服务器与 WEB 应用之间数据传输的公认标准，Java 中常见的 Json 类库有 Gson、JSON-lib 和 Jackson 等。
       jackson-core提供JsonParser和JsonGenerator可以实现基本的序列化和反序列化
       参考文档地址：https://blog.csdn.net/u011499747/article/details/78760405
    2、fastJson对于json格式字符串的解析主要用到了一下三个类：
       JSON：fastJson的解析器，用于JSON格式字符串与JSON对象及javaBean之间的转换。
       JSONObject：fastJson提供的json对象。
       JSONArray：fastJson提供json数组对象。
    3、GSON是Google提供的用来在Java对象和JSON数据之间进行映射的Java类库。
       可以将一个Json字符转成一个Java对象，或者将一个Java转化为Json字符串。
       特点： a、快速、高效  b、代码量少、简洁  c、面向对象  d、数据传递和解析方便
       Serialization:序列化，使Java对象到Json字符串的过程。
       Deserialization：反序列化，字符串转换成Java对象
*note.008:
    spring5（springboot2）开始用Caffeine缓存取代guava
    参考文档地址：https://blog.csdn.net/qq_35981283/article/details/82354081
*note.009:
    Kafka:高吞吐量的分布式发布订阅消息系统，可以处理消费者规模的网站中的所有动作流数据。
    这种动作（网页浏览，搜索和其他用户的行动）是在现代网络上的许多社会功能的一个关键因素。
    这些数据通常是由于吞吐量的要求而通过处理日志和日志聚合来解决。
    目的是通过Hadoop的并行加载机制来统一线上和离线的消息处理，也是为了通过集群来提供实时的消息。
    参考文档地址：https://baike.baidu.com/item/Kafka/17930165?fr=aladdin
*note.010:
    Springfox与swagger的整合使用
    1、一个基于Spring的组件swagger-springmvc，用于将swagger集成到springmvc中来。
       而springfox则是从这个组件发展而来，同时springfox也是一个新的项目。
       pringfox-swagger2依然是依赖OSA规范文档，也就是一个描述API的json文件，该组件的功能就是帮助自动生成这个json文件，
       *另外一个组件springfox-swagger-ui就是将这个json文件解析出来，用一种更友好的方式呈现出来。
    2、Swagger 是一个流行的API开发框架，这个框架以“开放API声明”（OpenAPI Specification，OAS）为基础，
       对整个API的开发周期都提供了相应的解决方案，是一个非常庞大的项目（包括设计、编码和测试，几乎支持所有语言）。
       OAS本身是一个API规范，它用于描述一整套API接口，包括一个接口是GET还是POST请求啊，有哪些参数哪些header等，都会被包括在这个文件中。
       它在设计的时候通常是YAML格式，这种格式书写起来比较方便，而在网络中传输时又会以json形式居多，因为json的通用性比较强。
       参考文档地址：https://www.cnblogs.com/getupmorning/p/7267076.html
                    https://blog.csdn.net/wsh900221/article/details/80508548
*note.011:
    使用shiro-redis集群
    使用了nginx做项目集群以后，就会出现严重问题：tomcat集群之间如何实现session共享的问题.不解决，就会出现登陆过后再次请求资源依旧需要登陆的问题
    spring和shiro集成：参考文档地址：http://www.cnblogs.com/hafiz/p/7226388.html
*note.012:
*note.013:
*note.014:
*note.015:
*note.016:
*note.017:
*note.018:
*note.019:
*note.020:
*note.021:
*note.022:
*note.023:

英语词汇

basic   基本
gateway 网关
config  配置
portal  入口
build-tools  工具

ak 公钥  ak解析
sk 秘钥  不在网络中传输(安全性)
timestamp
sign 签名 = md5(ak + timestamp)

Add Xsi(xml schema instance) schema location for external resource  为外部资源添加Xsi（xml架构实例）架构位置
Fetch\Ignore\Manually external resource  获取外部资源\忽略外部资源\手动设置外部资源

web-app是web.xml的根节点标签名称
version是版本的意思
xmlns是web.xml文件用到的命名空间
xmlns:xsi是指web.xml遵守xml规范
xsi:schemaLocation是指具体用到的schema资源
不要看相关中文资料，看了就不明白了，schema就是schema你把他翻译成对文档的限制就行了。你可能会说，dtd才是，实际上xsd和dtd是一样的

